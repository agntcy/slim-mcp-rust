apiVersion: v2
name: mcp-proxy
description: A Helm chart for deploying SLIM MCP Proxy

type: application

version: 0.1.0

appVersion: "0.2.1"

keywords:
  - mcp
  - proxy
  - slim
  - model-context-protocol
maintainers:
  - name: AGNTCY Contributors
    url: https://github.com/agntcy

# Uncomment to use SLIM as a dependency (requires Helm repository or OCI registry setup)
# dependencies:
#   - name: slim
#     version: "~1.1.0"
#     repository: "https://your-helm-repo/charts"
#     condition: slim.enabled
